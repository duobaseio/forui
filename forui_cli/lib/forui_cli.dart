/// A codec for encoding and decoding Forui theme presets, shared by the CLI and the create application.
///
/// This barrel is web-safe; it pulls in no `dart:ffi`.
library;

export 'src/codec.dart';
